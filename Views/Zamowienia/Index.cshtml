@model IEnumerable<Hypta_Projekt.Models.Zamowienie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataZamowienia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusZamowienia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produkt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KlientID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klient)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataZamowienia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusZamowienia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Produkt.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Klient.KlientID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Klient.Nazwisko)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ZamowienieID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ZamowienieID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ZamowienieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
