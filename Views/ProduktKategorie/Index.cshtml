@model IEnumerable<Hypta_Projekt.Models.ProduktKategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kategoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produkt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kategoria.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Produkt.Nazwa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProduktKategoriaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProduktKategoriaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProduktKategoriaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
